import styled from 'styled-components'

export const ButtonStyled = styled.button`
  border: 0;
  cursor: pointer;
  outline: none;

  &:hover {
    opacity: 0.6;
  }

  &:active {
    font-size: 0.9rem;
  }
`
